/**
 * Expo push settings — must match mobile `constants/expoPush.ts`.
 */
export const EXPO_PUSH_CONFIG = {
  projectId: '0bb1b8ef-f01a-415a-9639-7788882cd311',
  /** `@owner/slug` of the project above — Expo names tokens by this. */
  experienceId: '@alaahameds-team/3elagi1',
  apiUrl: 'https://exp.host/--/api/v2/push/send',
} as const;
